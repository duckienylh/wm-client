query Login($input: UserLoginInput!) {
    login(input: $input) {
        token
        user {
            id
            email
            userName
            phoneNumber
            firstName
            lastName
            fullName
            address
            avatarURL
            isActive
            role
            createdAt
            updatedAt
        }
    }
}
