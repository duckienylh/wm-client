query GetProductById($productId: Int!){
    getProductById(productId: $productId){
        id
        name
        category {
            id
            name
        }
        code
        price
        quantity
        height
        width
        weight
        image
        description
        createdAt
        updatedAt
    }
}
